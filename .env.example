# ═══════════════════════════════════════════════════════════
# ClawTrader — Environment Variables
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════

# ── Neon Postgres (Required) ─────────────────────────────────
# Get from: https://console.neon.tech → Project → Connection Details → Connection string
VITE_DATABASE_URL=postgresql://username:password@host.neon.tech/neondb?sslmode=require

# ── Trading Server (Optional) ────────────────────────────────
# URL of the trading server (default: http://localhost:3001)
VITE_TRADING_SERVER_URL=http://localhost:3001

# ── Trading Server Wallet (server/.env or environment) ───────
# Private key of the operator wallet used by the trading server
TRADING_WALLET_PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# ── Polygon Amoy RPC (Optional — defaults to public endpoint) ─
POLYGON_AMOY_RPC_URL=https://polygon-amoy.drpc.org
